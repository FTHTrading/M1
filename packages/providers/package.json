{
  "name": "@treasury/providers",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --project tsconfig.json",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@treasury/config": "workspace:*",
    "@treasury/types": "workspace:*",
    "zod": "^3.23.4"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.5.0",
    "@types/node": "^20.12.7"
  }
}
